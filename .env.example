# 🔒 ENVIRONMENT CONFIGURATION
# Copy this file to .env.local and fill in your values

# =====================================================
# 🚨 SECURITY CRITICAL: These are SERVER-SIDE ONLY
# Never prefix these with VITE_ (client exposure risk)
# =====================================================

# AI Services (Server-side only)
CLAUDE_API_KEY=your_claude_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Database (Server-side only)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Internal Security
INTERNAL_API_KEY=generate_secure_random_string_here
SECURITY_WEBHOOK_URL=https://hooks.slack.com/your-security-webhook

# Rate Limiting (Production)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# =====================================================
# ✅ PUBLIC VARIABLES: These are safe to expose
# These can have VITE_ prefix for client-side access
# =====================================================

# Application Info
VITE_PUBLIC_APP_NAME=theBlockchain.ai
VITE_PUBLIC_APP_URL=https://theblockchain.ai

# Supabase Public
VITE_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
VITE_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Analytics (Public)
VITE_PUBLIC_POSTHOG_KEY=your_posthog_key_here

# =====================================================
# 🔧 DEVELOPMENT SETTINGS
# =====================================================

NODE_ENV=development
# NODE_ENV=production

# Development CORS (only in development)
VITE_DEV_CORS_ORIGIN=http://localhost:5173

# =====================================================
# 📋 SETUP INSTRUCTIONS
# =====================================================

# 1. Copy this file to .env.local
#    cp .env.example .env.local

# 2. Generate secure internal API key:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 3. Get Supabase keys from: https://app.supabase.com/project/your-project/settings/api

# 4. Get Claude API key from: https://console.anthropic.com/

# 5. Get Gemini API key from: https://aistudio.google.com/

# 6. Set up Upstash Redis: https://console.upstash.com/

# 7. Configure Netlify environment variables with the SERVER-SIDE values

# =====================================================
# ⚠️  SECURITY REMINDERS
# =====================================================

# ❌ NEVER commit .env.local to git
# ❌ NEVER use VITE_ prefix for sensitive data
# ❌ NEVER expose API keys in client-side code
# ✅ Always validate inputs on server-side
# ✅ Use rate limiting on all public endpoints
# ✅ Monitor security events and anomalies
